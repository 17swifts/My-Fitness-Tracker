import React from 'react';
import { Typography, Box } from '@mui/material';

const Statistics = () => {
  return (
    <Box p={3}>
      <Typography variant="h4" gutterBottom>
        Statistics
      </Typography>
      {/* Insert statistics component here */}
    </Box>
  );
};

export default Statistics;
